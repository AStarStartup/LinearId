// I don't think I need this file. I'm not sure why they put it in the template.
/*
import { LIDNextMSW, RandomBigInt, LIDSource, LIDNext, LIDNextBuffer, LIDMilliseconds, LIDSeconds, LIDString, BigIntToHex, LIDPrint, HexToBigInt, LIDParse } from './';

export { LIDNextMSW, RandomBigInt, LIDSource, LIDNext, LIDNextBuffer, LIDMilliseconds, LIDSeconds, LIDString, BigIntToHex, LIDPrint, HexToBigInt, LIDParse };
*/

export * from 'linearid';
